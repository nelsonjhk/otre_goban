otre.display.environment_test = function() {
  var util = otre.util,
      display = otre.display,
      enums = otre.enums,
      env = otre.display.environment;
  test("--------Gui Environment Tests--------", function() { ok(true); });
};
